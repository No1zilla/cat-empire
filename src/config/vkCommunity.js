/**
 * Паблик «Двор империи». Подставь реальный group_id, когда паблик будет.
 * Если 0 — кнопка есть, вступление не начисляет пайку.
 */
export const VK_GROUP_ID = 0;

export default { VK_GROUP_ID };
