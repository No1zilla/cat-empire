# 📌 TASK-034: Модерация VK: Обучение новичков (Onboarding) и Safe Area (Мобильная верстка)

> **Статус:** ✅ Завершено
> **Приоритет:** Высокий

---

## 🎯 Описание задачи
Модераторы VK проверяют понятность игрового процесса для нового пользователя и отсутствие залезания элементов интерфейса под шторку/челку смартфона (Safe Area).

---

## 🛠 Выполненные задачи:
1. **Первичный Туториал (Onboarding)**:
   - Исправлена рассинхронизация координат подсветки (spotlight) и анимированной руки 👆.
   - Шаг 1: Рука и подсветка ровно указывают на кнопку «🐱 Купить» `(x=12, y=483)`.
   - Шаг 2: Рука анимированно перетаскивается с ячейки 0 на ячейку 1, показывая механику слияния.
   - Шаг 3: Подсветка указывает на созданного нового котика.
   - Добавлен метод `window.resetTutorial()` в консоли браузера для повторной проверки.
2. **Адаптация под Safe Area и Челки (Notch)**:
   - В `index.html` добавлен метатег `viewport-fit=cover`.
   - В `#game-container` добавлены отступы `env(safe-area-inset-top)` и `env(safe-area-inset-bottom)` для исключения захода элементов под шторку iOS/Android в VK Mini App.
